import React, { Component, PropTypes } from 'react';

export default class QuestionItem extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div>MyComponent</div>
    );
  }
}
